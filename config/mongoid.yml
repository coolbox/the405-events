development:
  sessions:
    default:
      database: the405-events
      hosts:
        - localhost:27017
      options:
        skip_version_check: true
        safe: true
production:
  sessions:
    default:
      uri: <%= ENV['MONGOHQ_URL'] %>
      options:
        skip_version_check: true
        safe: true